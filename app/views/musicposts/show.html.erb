<div class="row">
  <div class="col-md-3 musicpost_detail_left">
    <%= image_tag(@musicpost.user.display_avatar, class:"img_avatar") %>
    <p><%= link_to @musicpost.user.name, @musicpost.user %></p>
    <% unless @musicpost.user == current_user %>
      <%= render 'users/follow_form', user: @musicpost.user %>
    <% end %>
    <div class="musicpost_button">
      <ul>
        <li><%= render 'shared/play_pause', musicpost: @musicpost %></li>
        <% if user_signed_in? %>
          <li><%= render 'musicposts/favorite_form', musicpost: @musicpost %></li>
        <% end %>
      </ul>
    </div>
  </div>
  <div class="col-md-9 musicpost_detail_right">
    <h1><%= @musicpost.title %></h1>
    <ul>
      <li>作曲家：<%= @composer %></li>
      <li>演奏楽器：<%= @instrument %></li>
      <li>概要：<%= @musicpost.overview %></li>
      <li>アップロード：<%= time_ago_in_words(@musicpost.created_at) %>前</li>
    </ul>
  </div>
</div>
<div class="row">
  <div class="col-md-12 user_tab tab">
    <ul>
      <li class="comment_list"><%= link_to "コメント", '' %></li>
      <li class="memo_list"><%= link_to "メモ", '' %></li>
    </ul>
  </div>
</div>
